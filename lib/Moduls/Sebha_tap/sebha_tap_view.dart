import 'package:flutter/material.dart';

class SebhaTapView extends StatelessWidget {
  const SebhaTapView({super.key});

  @override
  Widget build(BuildContext context) {
    return Container(
      color: Colors.green,
    );
  }
}
