import 'package:flutter/material.dart';

class QuranTapView extends StatelessWidget {
  const QuranTapView({super.key});

  @override
  Widget build(BuildContext context) {
    return Container(
      color: Colors.orange,
    );
  }
}
